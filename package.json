{
  "name": "opentransit",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "lint:all": "yarn --cwd=frontend lint:all",
    "build": "yarn --cwd=frontend build",
    "start:python": "FLASK_APP=metrics-api.py flask run --host 0.0.0.0"
  }
}
